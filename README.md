# ✨ TelegramNameUpdate (动态名字更新面板)

一个轻量、优雅且高度自动化的 Telegram 姓氏（Last Name）动态更新工具。
不仅能让你的名字实时显示**时间、日期、当地温度与天气状态**，还附带了一个极客风的**终端交互面板 (TUI)**，让你在服务器上可以像使用普通软件一样轻松管理它。

完美兼容原生 Debian 13 / Ubuntu 等启用了 PEP 668 环境隔离保护的现代 Linux 系统。

## 🌟 功能特性

- **🕒 实时同步**：每分钟精确跳动，支持显示当前时间与日期。
- **⛅ 天气联动**：自动拉取指定城市的实时温度，并匹配对应的天气 Emoji（如 ☀️、🌧️、❄️）。
- **🔠 粗体美化**：自动将时间与温度的普通数字转换为 Unicode 无衬线粗体（如 `𝟭𝟰:𝟯𝟬 𝟮𝟱°C ☀️`），视觉效果更佳。
- **🖥️ 交互面板**：无需修改代码，终端输入 `tg` 即可唤出可视化管理面板，0-11 选项一键开关各项功能。
- **🛡️ 纯净隔离**：全自动配置 Python `venv` 虚拟环境，不污染宿主机系统环境。
- **⚙️ 守护进程**：使用 `systemd` 托管，后台静默运行，支持开机自启与崩溃自动重启。
- **🔄 无损热更新**：面板自带远程版本检测机制，按一个键即可从 GitHub 拉取最新代码并自动重启，不丢失任何本地个性化配置。

---

## 🚀 一键安装 (推荐)

在支持 `systemd` 的 Debian / Ubuntu 系统终端中，以 `root` 或 `sudo` 权限直接运行以下命令即可完成自动部署：

```bash
bash <(curl -sL [https://raw.githubusercontent.com/oKafuChino/TelegramNameUpdate/main/install.sh](https://raw.githubusercontent.com/oKafuChino/TelegramNameUpdate/main/install.sh))
```

> **⚠️ 准备工作**：安装前，请确保你已经前往 [Telegram API 申请页面](https://my.telegram.org) 获取了属于你账号的 `api_id` 和 `api_hash`。

---
## 🎮 使用指南

安装完成后，在终端的任意目录下输入以下快捷命令即可呼出管理面板：

```bash
tg
```

### 面板功能一览：

- `[1]` **更新账号 Session**：首次使用或更换账号时，用于输入手机号获取登录凭证。
- `[2]` **查看运行日志**：快速调出后台服务最近的 50 条运行日志，方便排查错误。
- `[3] - [6]` **展示项开关**：独立控制 时间、日期、温度、天气 的显示与隐藏。
- `[7]` **设置地区**：输入你所在的城市名称（支持英文或拼音），用于精准获取天气。
- `[8]` **粗体显示**：开启后，名字中的所有数字将变成漂亮的特殊粗体字。
- `[9]` **一键全开**：懒人福音，一键开启所有展示模块。
- `[10]` **强制重启后台服务**：遇到网络波动卡顿时，用于快速重置后台运行状态。
- `[11]` **自动更新脚本**：自动比对 GitHub 最新版本号，实现代码的无损覆盖与热更新。

---
## 💡 常见问题 (FAQ)

### Q1：为什么我成功登录并运行了脚本，但我的大号看小号名字还是没变？
**A**：这是 Telegram 官方的**联系人备注覆盖机制**。在 Telegram 中，你给好友设置的本地备注优先级远高于对方的公网名字。如果你用大号看小号，无论脚本怎么改，大号看到的永远是你存的通讯录名字。
* **验证方法**：请使用一个完全没有加过好友的第三方账号去查看小号，或者在小号自己的“设置 -> 个人资料”中查看真实修改效果。

### Q2：如何进行版本升级？会丢失我的城市设置和登录状态吗？
**A**：完全不会。只需在终端输入 `tg` 打开面板，输入 `11` 即可触发**无损热更新**。脚本只会覆盖 `tg_panel.py` 和 `tg_daemon.py` 核心逻辑，故意不碰你的 `config.json` 和 `api_auth.session` 凭证，升级过程秒级完成，体验极其丝滑。

---

## 🙏 致谢 (Acknowledgments)

本项目在创作过程中，核心灵感与基础的自动更新逻辑参考了以下优秀开源项目，特此向原作者表示由衷的感谢：

- [xyou365/Telegram-Name-Updating](https://github.com/xyou365/Telegram-Name-Updating)

---

**如果这个项目觉得对你有帮助，欢迎点亮右上角的 ⭐️ Star！**